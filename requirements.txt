# Core dependencies for 3D mesh processing
pymeshlab>=2022.2
trimesh>=4.0.0
pillow>=10.0.0
numpy>=1.24.0
pyyaml>=6.0
tqdm>=4.65.0
requests>=2.31.0

# PyTorch - Install via setup.sh for proper CUDA support
# torch>=2.0.0
# torchvision>=0.15.0

# Real-ESRGAN for texture enhancement
# Option 1: Use official realesrgan (requires basicsr)
# realesrgan>=0.3.0
# basicsr>=1.4.2
# facexlib>=0.3.0
# gfpgan>=1.3.8

# Option 2: Lightweight alternative using ISR (Image Super Resolution)
# ISR is easier to install and doesn't require basicsr
ISR>=2.2.0

# Optional: Stable Diffusion enhancement
# Uncomment to enable SD enhancement features
# diffusers>=0.21.0
# transformers>=4.30.0
# accelerate>=0.20.0

# Additional mesh processing dependencies
networkx>=3.0
scipy>=1.10.0
